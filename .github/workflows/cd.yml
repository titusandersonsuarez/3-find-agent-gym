name: CD

on:
  workflow_dispatch:
    inputs:
      check_azure:
        description: "Comprobar la conexión OIDC con Azure (no despliega)"
        required: true
        type: boolean
        default: false
  push:
    branches: [ main, master ]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore

      - name: Publish app
        run: dotnet publish LeadGym.AI.csproj -c Release -o publish

      - name: Upload published artifact
        uses: actions/upload-artifact@v4
        with:
          name: leadgym-ai-artifact
          path: publish/

  azure-connection-check:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.check_azure == true }}
    runs-on: ubuntu-latest

    steps:
      - name: Login to Azure with OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Verify Azure subscription
        run: az account show --output table
